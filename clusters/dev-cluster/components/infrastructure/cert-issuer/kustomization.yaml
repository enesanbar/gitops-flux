apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- mkcert-issuer.yaml
# The CA secret (mkcert-ca-key-pair) is intentionally NOT in git: it is
# per-machine key material, created by scripts/flux/install-mkcert-ca.sh.
