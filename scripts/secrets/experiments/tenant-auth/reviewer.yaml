# Tenant-side objects for Kubernetes auth from a Vault outside the cluster: a ServiceAccount allowed
# to call TokenReview and a long-lived token for it. In a real platform the tenant applies this and
# hands the token over; the lab helper applies it because the tenant here is a throwaway.
apiVersion: v1
kind: ServiceAccount
metadata: {name: vault-token-reviewer, namespace: kube-system}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata: {name: vault-token-reviewer}
roleRef: {apiGroup: rbac.authorization.k8s.io, kind: ClusterRole, name: system:auth-delegator}
subjects: [{kind: ServiceAccount, name: vault-token-reviewer, namespace: kube-system}]
---
apiVersion: v1
kind: Secret
metadata:
  name: vault-token-reviewer
  namespace: kube-system
  annotations: {kubernetes.io/service-account.name: vault-token-reviewer}
type: kubernetes.io/service-account-token
