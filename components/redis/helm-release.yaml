apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis
  namespace: redis
spec:
  interval: 1h
  releaseName: redis
  chart:
    spec:
      chart: redis
      version: "23.1.1"
      sourceRef:
        kind: HelmRepository
        name: bitnami-oci
        namespace: flux-system
