apiVersion: v1
kind: Namespace
metadata:
  name: onyx
  annotations:
    # Holds the out-of-git Secrets from scripts/flux/install-onyx-secrets.sh and
    # the database's claim; pruning it would take both with it.
    kustomize.toolkit.fluxcd.io/prune: disabled
