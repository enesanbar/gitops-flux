apiVersion: v1
kind: Namespace
metadata:
  name: pgadmin
  annotations:
    # Holds the out-of-git Secrets from scripts/flux/install-pgadmin-secrets.sh
    # (the admin login password and the per-database passwords); pruning the
    # namespace would take both with it.
    kustomize.toolkit.fluxcd.io/prune: disabled
