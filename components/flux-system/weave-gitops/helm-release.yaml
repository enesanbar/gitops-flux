apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: weave-gitops
  namespace: flux-system
spec:
  interval: 1h
  releaseName: weave-gitops
  chart:
    spec:
      chart: weave-gitops
#      version: "4.0.36"
      sourceRef:
        kind: HelmRepository
        name: ww-gitops
        namespace: flux-system
